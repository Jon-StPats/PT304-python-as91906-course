## Checkpoint 6 — Robustness + Testing

You are ready to start the AS91906 project when you can:
- validate input
- handle ValueError and FileNotFoundError
- avoid hard-coded "magic numbers" by using constants
- record tests (expected, boundary, invalid)
